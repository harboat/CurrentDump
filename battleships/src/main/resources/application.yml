server:
  port: 8081
  error:
    include-message: always

spring:
  application:
    name: battleships
  data:
    mongodb:
      uri: ${mongo_uri}
  rabbitmq:
    addresses: ${rabbit_uri}
    username: ${rabbit_username}
    password: ${rabbit_password}
  multirabbitmq:
    enabled: true
    connections:
      logger:
        addresses: ${rabbit_logger_uri}
        username: ${rabbit_logger_username}
        password: ${rabbit_logger_password}

rabbitmq:
  exchanges:
    game: internal-game.exchange
    core: internal-core.exchange
    notification: internal-notification.exchange
  queues:
    game-creation: game-creation.queue
    game-placement: game-placement.queue
  routing-keys:
    game-creation: internal.game-creation.routing-key
    game-placement: internal.game-placement.routing-key
    notification: internal.notification.routing-key
    core-game-creation-response: internal.core-game-creation-response.routing-key
